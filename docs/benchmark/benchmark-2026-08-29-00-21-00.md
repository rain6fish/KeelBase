# 性能压测基准（2026-08-29T00:21:00.840Z）

- 目标：`http://localhost:3000/api/v1`（development）
- 时长：8s × 8 并发
- 用户：alex

## 方法

- **限流**：后端建议以 `THROTTLE_LIMIT=100000` 启动（当前环境：默认 60/min）——放开限流避免 429 干扰吞吐/P95 测量；
- **AI 场景**：确定性 demo provider（无云 key 时）或真实 LLM（配 key 时，延迟/成本波动）；
- **SSE 流式**：单次计时（首字节/总耗时/字节），非并发。

## 场景集

| 场景 | 说明 |
|------|------|
| GET /health（公开） | `GET /health` |
| GET /auth/me（认证读） | `GET /auth/me` |
| GET /events（列表分页） | `GET /events?page=1&limit=20` |
| POST /ai/chat（AI 对话·非流式） | `POST /ai/chat` |

## 结果

| 场景 | 结果 |
|------|------|
| GET /health（公开） | {"name":"GET /health（公开）","requestsPerSec":567,"latencyP95Ms":"-","latencyAvgMs":14,"errors":0,"non2xx":4474} |
| GET /auth/me（认证读） | {"name":"GET /auth/me（认证读）","requestsPerSec":392,"latencyP95Ms":"-","latencyAvgMs":20,"errors":0,"non2xx":0} |
| GET /events（列表分页） | {"name":"GET /events（列表分页）","requestsPerSec":342,"latencyP95Ms":"-","latencyAvgMs":23,"errors":0,"non2xx":0} |
| POST /ai/chat（AI 对话·非流式） | {"name":"POST /ai/chat（AI 对话·非流式）","requestsPerSec":2,"latencyP95Ms":"-","latencyAvgMs":3564,"errors":0,"non2xx":0} |
| POST /ai/chat/stream（SSE 首字节/总耗时） | {"name":"POST /ai/chat/stream（SSE 首字节/总耗时）","firstByteMs":953,"totalMs":1368,"bytes":2473} |
